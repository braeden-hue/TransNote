# TransNote 웹페이지 기능 정리

이 문서는 지금까지 개발된 웹페이지(`webpage/`)의 **기능만** 정리한다 — 어떤 버튼이
있고, 그 버튼을 누르면 어떤 서비스가 제공되는지만 다룬다(디자인/레이아웃 설명 제외).

---

## 1. 랜딩 화면

첫 진입 화면. 4개의 버튼(핫스팟)으로 각 기능에 진입한다.

| 버튼 | 서비스 |
|---|---|
| **TransNote** | AI 모델이 어떻게 악보를 인식하는지 설명하는 화면으로 이동(8번 항목 참고) |
| **튜토리얼** | 커스텀 악보 표기법을 단계별로 배우는 흐름 시작(2번 항목) |
| **프로젝트에 대해** | 프로젝트 소개 팝업 표시 |
| **체험하기** | 샘플/촬영 악보로 실제 인식→변환→연주까지 체험하는 흐름 시작(3번 항목) |

---

## 2. 튜토리얼

커스텀 악보 표기법을 처음 배우는 사람을 위한 단계별 학습 화면. **이전 / 다음** 버튼으로
페이지를 넘기며, "처음으로" 버튼으로 언제든 랜딩으로 나갈 수 있다. 총 10페이지로
구성된다.

1. **도입 비교** — 기존 오선 악보와 커스텀 악보를 나란히 놓고 비교
2. **규칙 0** — 한 옥타브(흰건반 도~시, 검은건반 1~5)를 직접 눌러서 소리를 들어봄
3. **규칙 1: 세로 위치 = 음 높이** — 위쪽 존은 오른손(높은음자리), 아래쪽 존은 왼손
   (낮은음자리)이며, 각 존을 누르면 해당 음이 재생됨
4. **규칙 2: 셀 너비 = 음 길이, 테두리 색 = 박자 위치** — 넓은 칸일수록 긴 음, 색으로
   1~4박 중 몇 번째 박자인지 구분
5. **규칙 3: 화음** — 두 음을 동시에 눌러야 하는 화음 표기를 실습
6~10. **퀴즈 5문항** — 힌트 없이 화면에 뜬 악보를 보고 정답 건반(또는 화음, 음 순서대로
   치기)을 직접 눌러 맞히는 테스트

규칙 2 이후 페이지부터는 화면 한쪽에 **🔍 참고표** 버튼이 떠서, 누르면 규칙 0의 12음
라벨표를 다시 팝업으로 볼 수 있다(체험하기 연주 화면에서도 동일하게 제공).

---

## 3. 체험하기

방문객이 직접 곡 하나를 골라 촬영/변환하고, 연주까지 해보는 체험형 흐름. 5단계로
구성된다.

### 3-1. 곡 선택
- **샘플 5곡 버튼**(나비야 나비야 🦋 · 도레미송 🎵 · 수업 시작이다 🔔 · 크리스마스 🎄 ·
  메이플스토리 🍁) 중 하나를 누르면 그 곡의 변환된 커스텀 악보로 바로 이동
- **직접 촬영 버튼**(📷) — 카메라로 실제 악보를 찍어서 변환(4번 항목 "카메라 촬영" 참고)

### 3-2. 곡 미리보기
- 선택한 곡의 첫 마디를 커스텀 악보로 보여줌
- **▶ 연주 듣기** 버튼 — 화면(또는 연결된 전자 피아노)으로 곡을 자동 재생해서 들려줌
- **🎹 연주하기** 버튼 — 직접 연주 체험으로 진입
- 화면 옆에 그 곡의 **TOP 3 리더보드**(닉네임+점수) 표시

### 3-3. 손 모드 선택
- **오른손만**(100점 만점) — 오른손 파트만 연주
- **양손**(150점 만점) — 양손 파트를 함께 연주(대보표 곡만 선택 가능, 단일 오선 곡은
  이 버튼이 비활성화됨)

### 3-4. 연결 확인 + 닉네임
- 전자 피아노(MIDI)가 연결돼 있으면 화면에 안내된 음을 실제로 눌러 연결을 확인
- 감지된 기기 이름, 신호 수신 여부를 화면에 실시간 표시
- 확인이 안 되면 **🔄 다시 확인** 버튼으로 재시도 가능
- 닉네임 입력 후 **시작** 버튼으로 연주 화면 진입(연결이 안 됐으면 화면 가상 건반으로
  자동 전환)

### 3-5. 연주 진행 + 결과
- 곡 전체가 마디 단위로 자동 진행 — 정확한 건반(화면 가상 피아노 또는 실물 전자 피아노)을
  눌러야 다음 음으로 넘어감, 맞았을 때/틀렸을 때 즉시 시각 표시
- **💡 힌트 버튼** — 다음 눌러야 할 건반을 미리 보여줌(사용 시 최종 점수 -10점)
- 곡을 다 치면 최종 점수와 함께 결과 화면 표시, 리더보드에 자동 등록(**확인** 버튼으로
  곡 미리보기 화면으로 복귀)

---

## 4. 카메라 촬영

"체험하기"와 "악보 변환"(5번 항목) 양쪽에서 공통으로 쓰이는 촬영 기능.

- **오선 1개 / 대보표(2개)** 모드 전환 — 촬영할 악보가 한 줄짜리인지, 양손 두 줄짜리인지
  선택
- **📸 촬영** 버튼 — 사진을 찍어 서버(AI 모델)로 전송, 인식 결과를 커스텀 악보로 변환
- **🖼️ 갤러리에서 찾기** 버튼(체험하기 한정) — 카메라 대신 기존 사진 파일 선택
- **취소** 버튼 — 촬영 화면 닫고 이전 화면으로
- 체험하기에서는 촬영 직후 **원본 사진 vs 변환된 악보를 나란히 비교**하는 화면이 뜨고,
  **🔄 다시 촬영** / **✅ 이대로 사용할게요** 버튼으로 재촬영하거나 그대로 진행할 수 있음

---

## 5. 악보 변환

일반 브라우징 모드(하단 탭바)에서 쓰는 메인 변환 기능.

- **샘플 악보 선택** — 5곡 중 하나를 눌러 바로 변환 결과 확인
- **📂 파일 선택** / **📷 촬영하기** — 내 악보 이미지를 업로드하거나 직접 촬영해서 변환
- **인식 모델 선택** 드롭다운(현재 커스텀 모델 1종 제공) + 서버 연결 상태 표시
- 변환 결과 화면 — 인식된 음표 수·템포 표시, 좌우 화살표로 여러 줄(오선 그룹) 넘겨보기
- **💾 저장하기** — 변환 결과를 "새로 저장" 또는 (모델이 한 줄씩만 인식하므로 여러 줄짜리
  악보는) "기존 악보에 이어 붙이기"로 저장
- **📱 QR로 가져가기** — 저장된 결과를 QR 코드로 띄워서, 스마트폰으로 스캔하면 그 결과를
  폰에서 바로 볼 수 있음

---

## 6. 연주하기

저장해둔 악보로 실제 연습하는 화면.

- **악보 선택** 사이드바 — 저장된 악보 목록에서 하나 선택
- **▶ 미리듣기** — 곡 전체를 자동으로 들려줌(① 단계)
- **Wait Mode**(② 단계) — 정확한 건반을 눌러야만 다음 음으로 넘어가는 연습 모드
- ⏮ 이전 음 / ⏭ 다음 음 / ⏹ 처음으로 버튼
- **빠르기(BPM) 슬라이더** — 재생/연습 속도 조절
- **🎼 반주 모드** 토글 — 켜면 왼손(베이스) 파트가 자동 재생되고, 오른손만 직접 연습
- **🎹 전자 피아노 연결** 버튼 — MIDI 기기의 입력/출력을 선택해 실물 피아노로 연주·자동
  재생 모두 가능
- 옥타브 이동 버튼(◀ ▶), **음이름 표시** 토글
- 컴퓨터 키보드(A~J 등)로도 화면 건반 연주 가능

---

## 7. 내 악보함

변환해서 저장해둔 커스텀 악보들을 목록으로 모아 보여주는 화면 — 선택하면 연주하기
화면으로 이어짐.

---

## 8. AI 모델(TransNote) 소개

랜딩 화면 "TransNote" 버튼 → 그 안의 **"모델 설명 보기"** 버튼을 누르면 책을 펼친 형태로
모델 설명이 나온다.

- **어떻게 읽는가** — 음표를 하나씩 따로 인식한 뒤 규칙으로 재조립하는 대신, 오선 전체를
  한 번에 보면서 음의 흐름을 그대로 따라가며 다음 음을 예측하는 End-to-End 방식
- **왜 이 방식인가** — 전문가용 세부 기호보다 코드·멜로디의 흐름이 더 중요하다고 판단해서
  택한 설계
- **마르코프 체인** — 실제 작곡된 곡 약 9천여 개에서 "다음 음이 실제로 어디로 이동하는지"
  통계를 뽑아, 그 통계를 기반으로 학습용 악보를 자동 생성하는 데 사용
- **학습 데이터 3단계**
  1. 캡처 — 마르코프 체인 기반으로 생성한 무작위 악보
  2. 노이즈 — 실사 이미지에서 뽑은 기울기·곡면·그림자 등을 강도별로 합성
  3. 실사 — 실제로 촬영한 악보 이미지 1,600장
- **인식 정확도** — 주 타겟 장르(뉴에이지) 실사 테스트 기준 약 **96%**

---

## 부가 기능

- **PWA 지원** — 홈 화면에 추가하면 주소창 없는 독립 앱처럼 실행 가능
- **QR 데모 접속** — 데모 URL을 QR 코드로 즉시 생성해 스캔만으로 접속 가능
