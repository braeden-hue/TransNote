# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
~$*
*.mscz~
.mscbackup/

# IntelliJ / VS Code
*.iml
*.ipr
*.iws
.idea/
#.vscode/

# Python
__pycache__/
*.pyo
.pytest_cache/

# OMR 모델 체크포인트/가중치 (큰 바이너리 — 절대 커밋 금지)
train/*.pt
train/**/*.pt
train/checkpoints_by_epoch/
train/checkpoints_legacy/
*.onnx
*.tflite
/*.pt

# 배포용 스테이징 폴더 (train/checkpoints・tokenizer258.json의 사본 — 소스 아님, README만 추적)
train/deploy_bundle/*.json

# 학습/평가용 대용량 데이터 (합성 생성물 — 소스 아님)
train/data/
test/data/
train/local_dense_pool/

# 실사 이미지 자료 (git에 올리지 않음 — 로컬 전용)
realImage/

# 전시/기획용 디자인 자산 (원본 .mscz, 목업 이미지 — AI 학습 코드와 무관, 로컬 전용)
designKit/

# 평가 리포트(생성물)
reports/*.csv
train/eval_*_log.txt
train/eval_*_report.json

# 모바일 export 산출물 (export_tflite.py 출력 -- 소스 아님, 대용량, *.tflite는 이미
# 위에서 무시되지만 tokenizer 사본 등 나머지 파일도 같이 무시)
train/tflite_export/

# API 키 / 비밀 정보 - 절대 커밋 금지
*api_key*
*API_KEY*
secrets/

# 서드파티 Claude Code 스킬(uipro init으로 설치, ui-ux-pro-max 등) — 로컬 개발 도구일 뿐
# 프로젝트 소스가 아니라 커밋 안 함
.claude/skills/
.vercel
